# layer-punktfunk-client

The punktfunk streaming **client** candy: `punktfunk-client` and the headless
`punktfunk` CLI, installed from unom's signed pacman repo on Arch/CachyOS.

Sibling of `layer-punktfunk` (the host). Bootstrap commit; the candy lands by PR.
